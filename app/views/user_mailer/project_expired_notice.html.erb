<h1>Hi <%= @project.user.name || @project.user.username %></h1>

<p>We wanted to inform you that the project <strong><%=  @project.title %></strong> has met it's 
  expiration date and is no longer active. <%= link_to "View you project", project_url(@project),
  target: "_blank" %>.</p>